""" 
2️⃣
Using metadata.
1. scan through every record and check which one accepts the metadata.
2. store which ever as an object of name and command in a list or set
3. and send them them back with a generated id. the ID will reperesent the key while each object-in-list is the value.
RETURN LIST ⌛
"""

